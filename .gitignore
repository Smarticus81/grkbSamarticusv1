node_modules
dist
build
.turbo
*.tsbuildinfo
.env
.env.local
.env.*.local
coverage
.vitest
.DS_Store
*.log
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea

# Vite temp files
vite.config.ts.timestamp-*

# TypeScript incremental build cache (stale copies break clean builds)
*.tsbuildinfo
**/*.tsbuildinfo

# Security — never commit secrets or temp files
secs.txt
plan.md
_tmp_*
*.docx

# Runtime upload artifacts (user data, not source)
apps/api/uploads/
