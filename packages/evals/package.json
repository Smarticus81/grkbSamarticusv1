{
  "name": "@regground/evals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "eval": "tsx src/runner.ts",
    "eval:all": "tsx src/runner.ts --suite=all",
    "bench": "tsx src/validation-bench.ts",
    "bench:update": "tsx src/validation-bench.ts --update-baseline",
    "test": "tsx src/validation-bench.ts",
    "check": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@regground/core": "workspace:*",
    "yaml": "^2.3.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
